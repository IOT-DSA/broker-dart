name: dsbroker
version: 1.0.0
description: Dart based DSA Broker
dependencies:
  args: ^0.13.2
  bignum: ^0.1.0
  cipher: ^0.7.1
  logging: ">=0.9.0 <0.13.0"
  crypto: ^0.9.0
  dslink:
    git: https://github.com/IOT-DSA/sdk-dslink-dart.git
dev_dependencies:
  test: ^0.12.5
  browser: any
environment:
  sdk: ^1.9.0
executables:
  dsbroker: broker
